<?xml version="1.0" encoding="utf-8"?>
<!-- Phone-number chip in the ledger header. Reads as a piece of data rather
     than a caption, which is what it looked like in the eyebrow slot the
     other screens use for section labels. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="#FFFFFF"/>
    <stroke android:width="0.5dp" android:color="#EAE2D1"/>
    <corners android:radius="20dp"/>
</shape>
