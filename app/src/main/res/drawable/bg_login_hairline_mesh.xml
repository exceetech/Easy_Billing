<?xml version="1.0" encoding="utf-8"?>
<!-- New login background — pale cream gradient base + a subtle diagonal
     hairline mesh converging from each corner, replacing the busier
     low-poly faceted pattern / offset-tiles design. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@drawable/bg_login_champagne"/>
    <item android:drawable="@drawable/ic_login_hairline_mesh"/>
</layer-list>
