<?xml version="1.0" encoding="utf-8"?>
<!-- Import services status filter chip — matches the dashboard category rail pill:
     white fill, 1.5dp stroke, 8dp corners (teal stroke when selected). -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF"/>
            <stroke android:width="1.5dp" android:color="#0F6E56"/>
            <corners android:radius="8dp"/>
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF"/>
            <stroke android:width="1.5dp" android:color="#E4DCC8"/>
            <corners android:radius="8dp"/>
        </shape>
    </item>
</selector>
