<?xml version="1.0" encoding="utf-8"?>
<!--
    Inventory row — "Option I": stripe row + monogram avatar, a plain
    serif price on the right (no pill), plus two detail lines underneath
    the name — a muted "Category · Variant · HSN NNNN · unit" line, and
    a status/value line pairing the stock-state caption (colour-coded) on
    the left with the row's total stock value (units × avg. cost) on the
    right. No overflow button — the whole row is tappable and opens the
    action dialog. Left stripe and avatar share the same stable,
    random-looking accent per product (InventoryAdapter.rowPalette).

    Bound by InventoryAdapter: viewStripe, tvAvatar, tvName, tvCost,
    tvDetailLine, tvStockStatus, tvStockValue, viewRowDivider.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <View
            android:id="@+id/viewStripe"
            android:layout_width="4dp"
            android:layout_height="match_parent"
            android:background="#0F6E56" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingHorizontal="14dp"
            android:paddingVertical="13dp">

            <TextView
                android:id="@+id/tvAvatar"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:gravity="center"
                android:text="CB"
                android:textSize="11.5sp"
                android:fontFamily="@font/googlesans_semibold"
                android:textColor="#085041"
                android:background="@drawable/bg_credit_avatar"
                android:backgroundTint="#E1F5EE"
                android:includeFontPadding="false"/>

            <LinearLayout
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:layout_marginStart="11dp">

                <TextView
                    android:id="@+id/tvName"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/product_name"
                    android:textSize="13.5sp"
                    android:fontFamily="@font/googlesans_medium"
                    android:textColor="#1A1A18"
                    android:maxLines="1"
                    android:ellipsize="end"/>

                <TextView
                    android:id="@+id/tvDetailLine"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="3dp"
                    android:text="@string/xml_inventory_category_variant_placeholder"
                    android:textSize="10sp"
                    android:textColor="#9A8F79"
                    android:maxLines="1"
                    android:ellipsize="end"/>

                <TextView
                    android:id="@+id/tvStockStatus"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="5dp"
                    android:text="@string/xml_inventory_units_in_stock_placeholder"
                    android:textSize="10.5sp"
                    android:fontFamily="@font/googlesans_medium"
                    android:textColor="#3B6D11"
                    android:maxLines="1"
                    android:ellipsize="end"/>

                <!-- Retained so InventoryAdapter's ViewHolder findViewById
                     call doesn't need touching; unused/hidden. -->
                <TextView android:id="@+id/tvStockValue" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone"/>

            </LinearLayout>

            <TextView
                android:id="@+id/tvCost"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:text="0"
                android:textSize="17sp"
                android:fontFamily="serif"
                android:textColor="#1A1A18"
                android:includeFontPadding="false"
                android:singleLine="true"/>

        </LinearLayout>

    </LinearLayout>

    <View
        android:id="@+id/viewRowDivider"
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:layout_marginStart="18dp"
        android:background="#F1EBDD" />

    <!-- Retained so InventoryAdapter's ViewHolder findViewById calls for
         legacy ids don't need touching outside this file; unused/hidden
         by this row design. -->
    <TextView android:id="@+id/tvMeta" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone"/>
    <TextView android:id="@+id/tvGst" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone"/>
    <TextView android:id="@+id/tvStatus" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone"/>
    <TextView android:id="@+id/tvCategory" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone"/>
    <View android:id="@+id/chkSelect" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone"/>

</LinearLayout>
