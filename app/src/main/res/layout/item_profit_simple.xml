<?xml version="1.0" encoding="utf-8"?>

<!--
  Profit row — exact same shape as Inventory's item_inventory.xml: colored
  left stripe + monogram avatar, name + detail line, a status line, and a
  serif price on the right, with a hairline divider below. Colors are set
  per-row by ProfitAdapter (teal for a net-profitable product, red for a
  net-loss one), same as InventoryAdapter's rowPalette/stock-state coloring.

  Bound by ProfitAdapter: viewStripe, tvAvatar, tvName, tvQty (detail line),
  tvMargin (status line), tvProfit, viewRowDivider.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <View
            android:id="@+id/viewStripe"
            android:layout_width="4dp"
            android:layout_height="match_parent"
            android:background="#0F6E56"/>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingHorizontal="14dp"
            android:paddingVertical="13dp">

            <TextView
                android:id="@+id/tvAvatar"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:gravity="center"
                android:text="PR"
                android:textSize="11.5sp"
                android:fontFamily="@font/googlesans_semibold"
                android:textColor="#085041"
                android:background="@drawable/bg_credit_avatar"
                android:backgroundTint="#E1F5EE"
                android:includeFontPadding="false"/>

            <LinearLayout
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:layout_marginStart="11dp">

                <TextView
                    android:id="@+id/tvName"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Product name"
                    android:textSize="13.5sp"
                    android:fontFamily="@font/googlesans_medium"
                    android:textColor="#1A1A18"
                    android:maxLines="1"
                    android:ellipsize="end"/>

                <TextView
                    android:id="@+id/tvQty"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="3dp"
                    android:text="0 sold"
                    android:textSize="10sp"
                    android:textColor="#9A8F79"
                    android:maxLines="1"
                    android:ellipsize="end"/>

                <!-- Retained so ProfitAdapter's ViewHolder findViewById call
                     doesn't need touching; unused/hidden. -->
                <TextView android:id="@+id/tvMargin" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone"/>
            </LinearLayout>

            <TextView
                android:id="@+id/tvProfit"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:text="0"
                android:textSize="17sp"
                android:fontFamily="serif"
                android:textColor="#085041"
                android:includeFontPadding="false"
                android:singleLine="true"/>

        </LinearLayout>
    </LinearLayout>

    <View
        android:id="@+id/viewRowDivider"
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:layout_marginStart="18dp"
        android:background="#F1EBDD"/>

</LinearLayout>
