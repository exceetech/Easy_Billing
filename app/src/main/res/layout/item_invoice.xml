<?xml version="1.0" encoding="utf-8"?>
<!--
    Bill details line row — matches the premium invoice-creation row
    style (item_invoice_premium.xml): rounded initials tile, bold
    product name, "qty × rate" meta line, and a right-aligned line
    total. Bound by BillDetailsAdapter.

    Ids: tvAvatar, tvName, tvMeta, tvPrice, viewRowDivider
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingVertical="11dp">

        <TextView
            android:id="@+id/tvAvatar"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:gravity="center"
            android:text="BR"
            android:textColor="#0B5544"
            android:textSize="12sp"
            android:fontFamily="@font/googlesans_semibold"
            android:background="@drawable/bg_inv_avatar_green"
            android:layout_marginEnd="12dp"/>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvName"
                android:text="@string/purchase_field_product"
                android:textColor="#18181B"
                android:textSize="13sp"
                android:fontFamily="@font/googlesans_semibold"
                android:maxLines="1"
                android:ellipsize="end"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"/>

            <TextView
                android:id="@+id/tvMeta"
                android:text="2 × ₹10"
                android:textColor="#9A8F79"
                android:textSize="10.5sp"
                android:layout_marginTop="2dp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"/>
        </LinearLayout>

        <TextView
            android:id="@+id/tvPrice"
            android:text="₹0"
            android:textColor="#18181B"
            android:textSize="14sp"
            android:fontFamily="serif"
            android:includeFontPadding="false"
            android:layout_marginStart="8dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

    </LinearLayout>

    <View
        android:id="@+id/viewRowDivider"
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="#F1EBDD"/>

</LinearLayout>
