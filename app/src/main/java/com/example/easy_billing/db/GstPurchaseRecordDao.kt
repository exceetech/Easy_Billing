package com.example.easy_billing.db

// RETIRED: DAO for the removed `gst_purchase_records` table. See the note in
// GstPurchaseRecord.kt. Intentionally left as an empty stub (the build sandbox
// could not delete the file). Safe to delete from the repo manually.
