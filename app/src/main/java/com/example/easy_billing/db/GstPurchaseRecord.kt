package com.example.easy_billing.db

// RETIRED: the `gst_purchase_records` table was removed (it was never read on
// the device and its server reports — GSTR-3B / GST email — are unused). The
// authoritative GST purchase data lives in purchase_table / purchase_items.
//
// This file is intentionally left as an empty stub (the build sandbox could not
// delete it). Safe to delete from the repo manually.
